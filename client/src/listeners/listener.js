/* eslint-disable folders/match-regex */
// import { data } from '../../data.js';
import { addElementHandler } from '../handlers/handler.js';

document
  .getElementById('add-button')
  .addEventListener('click', addElementHandler);
